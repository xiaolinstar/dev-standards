{
  "project": "ai-todo",
  "rules": [
    {
      "id": "ai-todo-check-wechat",
      "tier": "check",
      "status": "active",
      "since": "2026-06-28",
      "patterns": [
        "pnpm check:wechat",
        "pnpm test:api",
        "cd apps/api && .venv/bin/python -m pytest",
        "cd apps/api && python -m ruff check"
      ],
      "note": "ai-todo monorepo CI parity (merged with global manifest via sync.sh permissions)"
    }
  ],
  "autoRun": {
    "block_instructions": [
      "Do not auto-run CD deploy, docker push, or miniapp upload against production."
    ]
  }
}
