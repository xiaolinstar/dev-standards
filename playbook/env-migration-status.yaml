# 环境变量迁移进度（人工更新 Status）
# 机器扫描：scripts/sync.sh env status

version: 1
blocked_until_done: true
note: 全部项目 local_config + vps_runtime 为 done 后，再启动 /healthz 与 uptime 探活迁移

projects:
  xiaolin-gateway:
    category: platform
    priority: 1
    vps_host: 101.34.78.2
    local_config: done
    vps_runtime: done
    github_l2: partial
    blockers:
      - GitHub CD SERVER_HOST 应指向 101.34.78.2（若在 Secrets 中仍为旧 IP 需人工更新）

  xiaolin-docs:
    category: content
    priority: 2
    upstream: 124.222.98.227:8080
    local_config: done
    vps_runtime: done
    vps_note: VPS 无 .env 为预期（compose 不读 env 文件）
    github_l2: partial
    blockers:
      - 核对 GitHub Variables DOCKER_BAIDU_ANALYTICS_ID

  xiaolin-life:
    category: content
    priority: 2
    upstream: 124.222.98.227:8081
    local_config: done
    vps_runtime: done
    vps_note: VPS 无 .env 为预期；COS 凭证在 ~/.cos.yaml（暂不纳入 config 备份）
    github_l2: partial
    blockers: []

  ai-todo:
    category: application
    priority: 3
    local_config: pending
    vps_runtime: unknown
    github_l2: documented
    blockers:
      - apps/api/.env* 与 staging / prod VPS 分别整理
      - docs/env 已为参考实现

  party-helper:
    category: application
    priority: 4
    local_config: pending
    vps_runtime: unknown
    github_l2: partial
    blockers:
      - docs/env 已存在；补齐 apps/api/.env.production

  drink-budget:
    category: application
    priority: 5
    local_config: pending
    vps_runtime: unknown
    github_l2: unknown
    blockers:
      - 缺 apps/api/.env.production.example（需新增模板后再迁移）
